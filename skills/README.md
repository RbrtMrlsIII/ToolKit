# skills/ — Loadable Agent Skills

Each skill is a folder containing `SKILL.md`.

Load the relevant skill **before** executing that phase.

See `SKILLS_INDEX.md` for the recommended load order and full catalog.

Core flow: observe → record → understand → classify → align → validate → endorse → advance
